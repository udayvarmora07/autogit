# Phase 1 P1-06 through P1-10 implementation evidence

Date: 2026-09-15

Original implementation source snapshot: `1406352345886c116ba7750369fc3a14df960b63`
Windows follow-up source snapshot: `c1a94d4902e13d454ef87eb9c5d394c604bea608`
The evidence-record commit may be newer than this source snapshot.
Platform exercised: Linux amd64; Windows amd64 and Darwin arm64 cross-builds

This bundle records the five trusted-core work items completed in this pass.
It does not promote the Phase 1 gate: provider capability work remains open,
and named owner acceptance is still required. Linux Landlock is now enforced by
the child wrapper when the kernel ABI is available, with a direct child-process
adversarial test.
The exact-source hosted matrix `34604368677` supplies native Linux/macOS/
Windows execution for the implemented namespace, job-object, and process-group
fallback tests. The current exact-SHA manual matrix `34682716776` passed all 20
jobs against `1406352345886c116ba7750369fc3a14df960b63`, including the native
Linux direct Landlock test and the Windows/macOS process-boundary checks. The
Windows AppContainer and parent-attestation follow-up is recorded below;
named platform-owner acceptance remains open.

## Windows AppContainer follow-up — 2026-09-15

Native validation is carried by core workflow run
`34935126986`, native Windows job `104271291760`, against the exact clean
source snapshot above. This follow-up closes the Windows implementation gap
recorded above; named platform-owner acceptance remains a separate release
gate.

- Windows creates a unique ephemeral AppContainer profile with zero declared
  capabilities, launches the verifier suspended through `STARTUPINFOEX` and
  `PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES`, attaches the Job Object before
  resume, and refuses to resume until the parent attests the child token.
- The parent independently checks the observed AppContainer state, exact
  package SID, low-integrity token, and zero token capabilities. Trusted
  evidence records these observations and rejects an AppContainer primitive
  without the independent attestation.
- Explicit allowlisted files/directories receive temporary read/execute ACLs;
  writes and unlisted reads are denied by the native test. The filesystem
  probe is deliberately network-free; the parent independently proves network
  denial by observing zero token capabilities.
- Cleanup restores the original owner/group and DACL ACE/protection semantics,
  then deletes the ephemeral profile. The native test permits only the
  documented Windows normalization of the system-managed
  `SE_DACL_AUTO_INHERITED` (`AI`) control bit; it remains strict about the
  owner, group, ACEs, and DACL protection state. AppContainer stdout/stderr use
  inherited regular temporary files so Go Windows child startup does not block
  while probing synchronous pipe handles; a path-based monitor preserves the
  configured output limit and output is collected after process exit.

## P1-04 — enforcement slice

- Descendant cleanup covers both process groups and Linux descendants that
  deliberately create a new session.
- Linux CPU, address-space, and file-size ceilings are applied with `prlimit`;
  an explicit process-count ceiling is also applied when requested. Isolated
  namespace runs apply these limits to the verifier inside bubblewrap so
  process accounting is scoped to the sandbox user namespace. The default
  process-bounded tier leaves Linux process-count unset because `RLIMIT_NPROC`
  is per-UID and would count unrelated processes owned by the caller.
- Linux filesystem-isolated and filesystem/network-isolated tiers are backed by
  a root-owned, non-writable bubblewrap executable from a fixed system path,
  user/pid/network namespaces, read-only runtime and allowlisted binds, and
  `--unshare-net` for network denial. Bubblewrap is required to assert that
  nested user namespaces are disabled, preventing the verifier from creating
  a second user-namespace boundary inside the achieved sandbox. The machine-readable
  capability report records this lockout and records the ABI-gated Landlock
  ruleset when the child wrapper successfully has the primitive available.
  `TestLandlockDirectEnforcement` proves that an unlisted hierarchy is denied
  after the ruleset is installed. Unsupported platforms fail closed for these stronger
  tiers. Windows reports AppContainer plus Job Object enforcement and requires
  parent-side token attestation. macOS reports the process-group fallback and
  unavailable resource ceilings rather than emulating them.
- Trusted verifier evidence now binds the achieved primitive, limitations, and
  capability observations into the evidence digest; reuse rejects a changed
  primitive even if the broad isolation-tier name is unchanged.
- Runtime coverage is in `internal/process/process_tree_unix_test.go`,
  `internal/process/resource_limits_test.go`, `internal/process/sandbox_test.go`,
  `internal/process/landlock_test.go`, and
  `internal/verification/isolation_test.go`.

## P1-06 — candidate-tree differential coverage

- `CompareTreeToSnapshot` compares Git's resulting tree against immutable
  expected bytes, normalized modes, and exact safe paths, and rejects duplicate,
  delete, control, newline, traversal, and unsupported entries.
- Transaction tests cover exact bytes without clean filters/hooks, executable
  modes, option-like and Unicode paths, sparse-checkout parent retention, and
  concurrent index/worktree changes.
- Repository/history tests cover linked worktrees, Unicode paths, LFS pointer
  fail-closed scanning, submodules/gitlinks, symlinks, binary content, and raw
  malformed newline paths. The policy for unsupported tree kinds is explicit
  rejection rather than reinterpretation.

## P1-07 — SHA-1/SHA-256 object formats

- Git object format is detected from `rev-parse --show-object-format`; object
  validation accepts only the repository's 40- or 64-hex-character format.
- SHA-256 transaction creation, candidate tree comparison, commit identity, and
  reachable-history scanning are exercised in real Git repositories. Existing
  state, event, provider, and lifecycle validators already accept both object
  ID lengths where the domain contract permits them.

## P1-08 — verifier executable/config identity

- Trusted registry construction requires an existing canonical executable and
  records its SHA-256 fingerprint. Execution rechecks the fingerprint.
- Linux `ExecRunner` opens and executes the verified executable object through
  `/proc/self/fd`; bubblewrap namespaces carry that object through
  `--ro-bind-fd`. Replacement between validation and execution is rejected.
- Incompatible custom launchers and other platforms use the
  `digest-rechecked-path` fallback and report that residual limitation
  explicitly in `TrustedEvidence.ExecutableBinding`.
- Trusted verifier configuration remains bounded, protected-state, no-symlink
  input and its canonical digest is included in verification evidence.

## P1-09 — policy semantics

- Tracking is explicitly three-state: `no`, `local`, and `yes` (with empty only
  for an unconfigured policy). The inconsistent `public` tracking value is
  rejected and no predicate treats it as enabled.
- Provider access and public publication require explicit `yes`; local tracking
  cannot authorize either. Table tests cover validation, merge, and all states.

## P1-10 — read-only command paths

- `status` and `logs` now resolve repository/state facts without bootstrapping
  state, and open an existing event database through a read-only SQLite handle.
  `plan`, config explain, and non-repair doctor retain their read-only paths.
- Tests verify missing-state calls create no directory, existing state bytes and
  modification time remain unchanged, repository HEAD/index remain unchanged,
  and malformed/unsupported read-only requests do not create state.

## Verification run

The following checks passed in this workspace:

```text
go test ./...
go test -race ./internal/db ./internal/events ./internal/security ./internal/verification ./internal/process
go vet ./...
staticcheck ./...
GOOS=windows GOARCH=amd64 go test -c ./internal/process
GOOS=windows GOARCH=amd64 go test -c ./internal/verification
GOOS=darwin GOARCH=arm64 go test -c ./internal/process
GOOS=darwin GOARCH=arm64 go test -c ./internal/verification
```

Cross-builds prove compilation only; they are not native runtime validation.

## Current-HEAD native matrix refresh — 2026-09-20

The manual full core workflow passed all 20 jobs in hosted run
`35492327966` against exact source commit
`f8ad8d5f61d046551c281f759809d607d6a9fe79`. The run included native Linux,
macOS, and Windows core tests; native Linux/macOS/Windows ARM and amd64
artifact smoke plus install lifecycle drills; all three process-boundary soak
jobs; the bounded fuzz corpus; reproducible release builds; dependency and
workflow policy; and security analysis. This refresh supersedes the older
current-tree matrix references for implementation evidence only.

The result verifies the executable implementation and its native test paths;
it does not itself supply the required named Phase 1/platform-owner exit
acceptance, so the Phase 1 gate remains a release-acceptance item.
